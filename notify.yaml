#- name: telegram
#  platform: telegram
#  api_key: !secret telegram_api_key
#  chat_id: !secret telegram_chat_id

- name: all
  platform: group
  services:
#    - service: telegram
    - service: ios_wethop
